[package]
name = "astra-verifone-ffi"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Safe Rust FFI bindings for the Verifone Point of Sale SDK"
repository = "https://github.com/astra-system/astra-service"

[dependencies]
libc = "0.2"
thiserror = "1"

[build-dependencies]
bindgen = "0.69"
