// Tailwind CSS v4 configuration for the Astra-Service kiosk UI.
//
// v4 is configured CSS-first: all design tokens are consumed from
// `@astra/design-tokens` via `@theme inline` in `src/styles/global.css`.
// The legacy v3 `theme` / `content` / `darkMode` options are ignored by v4,
// so this file is intentionally minimal.
export default {};
